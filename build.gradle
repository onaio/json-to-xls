apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.yammer.dropwizard', name: 'dropwizard-core', version: '0.6.2'
}

apply plugin: 'application'

mainClassName = 'com.spike.helloworld.HelloWorldService'

run {
    args 'server', 'hello-world.yml'
}
